= simple_form_for resource, url: url, html: { class: 'form-horizontal' } do |f|
  %fieldset
    = f.input :name
    = f.input :email
    = f.input :mobile
    = f.input :content, as: :text, input_html: { size: '20x10' }
    
    .control-group
      %label
      .controls= f.submit class: 'btn'